#!/usr/bin/env node
'use strict';

var exec = require ('child_process').exec
  , path = require ('path')
  ;

var cmd = path.resolve (process.cwd (), 'node_modules/.bin/babel app -d dist');
console.log (cmd);

exec (cmd, function (error, stdout, stderr) {
  // command output is in stdout
});
